test('basic', () => {
  //
});

test('check setOrigin()', () => {
  //
});

test('check setVerbose()', () => {
  //
});

test('check setLogger()', () => {
  //
});

test('basic listen() with multi-eventListeners', () => {
  //
});

test('basic emit() with wait-for-response', () => {
  //
});

test('basic emit() with no-response', () => {
  //
});

test('basic with multi-iframe', () => {
  //
});
