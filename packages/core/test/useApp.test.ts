test('basic', () => {
  //
});

test('basic with props and attrs', () => {
  //
});

describe('check options', () => {
  test('bootstrap', () => {
    //
  });
  // TODO: add more test cases from options
});
